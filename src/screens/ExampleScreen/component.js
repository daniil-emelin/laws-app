import React, { Component } from 'react';
import { StackNavigator, TabNavigator, Header } from 'react-navigation';
import { View, Image, StyleSheet, Text, Platform, ImageBackground, TouchableOpacity, PlatformIOS } from 'react-native';

export default class ExampleScreen extends React.Component{
    render(){
        return(
            <View style={{flex:1}}><Text>test</Text></View>
        )
    }
}